// var nome = window.prompt('Olá! Qual o seu nome?')

// document.write(`Olá ${nome}! Seu nome tem ${nome.length} letras... <br>`)

// document.write(`Seu nome em letra maiúsculas é ${nome.toUpperCase()}...`)

/*
    Operadores Aritiméticos:

    '+' Adição;
    '-' Subtração;
    '*' Multiplicação;
    '/' Divisão;
    '%' Resto da Divisão;
    '**' Pontênciação;

        Ordem de Precedência Aritimética:
        1º -> '()' -> Operações dentro dos parênteses;
        2º -> '**'; 
        3º -> '*', '/', '%';
        4º -> '+', '-';

    Operadores de Atribuição:

    '=' Recebe:
        Se n recebe n mais alguma operação, ela pode ser simplificada:
            n = n + x -> n += x (no caso de 'n + 1' a operação pode ser ainda mais simplificada para n++ ou ++n);
            n = n - x -> n -= x (no caso de 'n - 1' a operação pode ser ainda mais simplificada para n-- ou --n);
            n = n * x -> n *= x;
            n = n / x -> n /= x;
            n = n % x -> n %= x;
            n = n ** x -> n **= x;
    
    Operadores Relacionais:
        Os resultados dos operadores relacionais são sempre booleanos, ou seja, 'true' ou 'false';

    '>' Maior;
    '<' Menor;
    '>=' Maior ou Igual;
    '<=' Menor ou Igual;
    '==' Igual;
    '!=' Diferente (Não Igual);

        Operadores de Identidade:   
            Verificam não só a igualdade dos valores mas também o tipo;

        '===' Estritamente Igual (Idêntico);
        '!==' Estritamente Diferente;

    Operadores Lógicos:
        Os resultados dos operadores relacionais são sempre booleanos, ou seja, 'true' ou 'false';

    '!' Negação (inverte/nega o valor da operação):
        true == !false;
        false == !true;

    '&&' Conjunção (é verdadeiro quando ambas as condições são verdadeiras):
        true && true = true;
        true && false = false;
        false && true = false;
        false && false = false;

    '||' Disjunção (é verdadeira quando pelo menos uma das condições são verdadeiras):
        true && true = true;
        true && false = true;
        false && true = true;
        false && false = false;


    Ordem de Precedência de Operadores:
        1º -> Artitiméticos;
        2º -> Relacionais (Identidade);
        3º -> Negação;
        4º -> Conjunção;
        5º -> Disjunção;

    Operador Ternário:
        Composto por -> 'teste lógico' ? 'se for true' : 'se for false';
                         teste ? true : false;

*/